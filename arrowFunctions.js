const esMayor = ({edad}) => {
    return edad >= 18
}
const esMayor = ({edad}) => edad >= 18 // Si la funcion solo retorna algo, se puede usar de esta manera

const suma = (a,b) => a + b

//Si es solo un parametro, no es necesario los parentesis
const esPar = n => n%2 == 0


const saluda = () => 'hola'